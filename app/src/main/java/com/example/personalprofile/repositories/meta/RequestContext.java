package com.example.personalprofile.repositories.meta;

// nothing in here, enforces that the thing passed into AbstractRepository is actually context for
// requests
public interface RequestContext {
}
